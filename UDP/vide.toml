#:schema https://vide.pascal-lab.net/schemas/v1/vide.schema.json
sources = ["**/*.v",  
    "**/*.sv"]

# include_dirs = ["."]
# defines = ["SYNTHESIS"]
# top_modules = ["top"]
# libraries = ["../common_cells"]
# exclude = ["build/**"]
